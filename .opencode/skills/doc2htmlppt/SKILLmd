# 技能名称: doc2htmlppt

## 描述
将飞书文档或任意文字稿转换为交互式HTML幻灯片。技能会自动分析内容，通过关键问题明确演示需求，最终生成一个支持翻页、响应式布局、可选特效的独立HTML文件，完美替代PPT。

## 触发条件
当用户发送以下任一内容时，可调用本技能：
- 飞书文档链接（公开可访问）
- 粘贴的文稿内容
- 明确要求“将文档转成HTML PPT”或类似表述

## 执行流程

### 1. 获取文档内容
- 如果用户提供飞书链接，尝试访问并提取正文（提示用户确保链接权限为“互联网上任何人可查看”）。若无法访问，要求用户手动粘贴内容。
- 如果用户直接粘贴文本，则直接使用。
- 若内容不完整，要求用户补充完整。

### 2. 分析内容并生成提问
阅读文档后，向用户一次性提出以下问题（可根据文档特性微调），并等待所有答案：

#### 基础信息
1. **场景与受众**
   - 这份材料主要用于什么场景？（内部汇报、客户提案、产品发布、培训等）
   - 目标听众是谁？（管理层、技术团队、客户、投资人等）
   - 最想让听众记住的3个核心点是什么？（用逗号分隔）

#### 内容处理
2. **内容提炼**
   - 是否需要对原始内容进行提炼和总结？（是/否，若“是”则自动精简文字，提取要点；若“否”则尽量保留原文细节）
3. **必须内容**
   - 有没有必须放的内容或数据？（如具体图表、原文、参数等，若无则填无）

#### 结构与布局
4. **页数与分页**
   - 希望的PPT大致页数？（如8-10页）
   - 每页内容展示方式：**“自适应分页”**（根据内容多少自动调整字号和分页）还是 **“固定页数”**（按您指定的页数均匀分配）？
5. **每页风格**
   - 倾向：“大字少话（演讲者补充）”还是“内容详细（自解释型）”？

#### 视觉与特效
6. **主配色**
   - 主配色是什么？（例如：科技蓝、生态绿、简约黑白、或直接提供色值如#3b82f6）
   - 是否有配色禁忌？（如避免红绿搭配，若无则填无）
7. **特效需求**
   - 是否需要翻页过渡特效？（是/否，若“是”可选：淡入淡出、滑动、缩放等）
   - 是否需要其他特效？（如文字动画、背景视差等，若无则填无）

#### 补充内容
8. **金句总结**
   - 是否需要一页“金句总结”升华结尾？（是/否）
9. **图表与数据**
   - 文档中是否包含需要可视化展示的数据？（是/否，若“是”请说明具体数据项）

#### 演讲辅助
10. **时长与关注点**
    - 预计汇报时长？（分钟，用于控制内容密度）
    - 听众最关心什么？（一句话概括）

> **额外建议**：根据文档内容，可追加1-2个针对性问题，例如“文档中的XX流程是否需要单独一页流程图？”或“是否需要添加演讲者备注？”

### 3. 生成HTML幻灯片
根据用户回答及文档内容，构建完整的HTML文件。具体实现可参考 `scripts/doc2htmlppt.py` 脚本，或直接使用脚本生成。脚本支持：
- 从飞书链接或本地文件读取内容
- 交互式提问（或通过命令行参数传递）
- 根据回答生成HTML，支持自适应/固定分页、特效、配色等

如果AI无法执行外部脚本，则应模拟脚本逻辑，按照以下原则生成：
- 使用 `assets/templates/base.html` 作为基础模板
- 根据用户回答填充内容、样式和特效
- 如需配色方案，参考 `assets/colors/palettes.json` 中的预设

### 4. 输出与说明
将完整的HTML代码输出到对话中，并提供保存说明：
- 如何保存为 `.html` 文件
- 如何全屏演示
- 提示用户可根据需要微调内容或样式
- 如果使用了外部库（如Chart.js），说明来源

## 技能文件说明
本技能包包含以下文件：
- `skill.md`：技能主文件（本文）
- `scripts/doc2htmlppt.py`：核心转换脚本（可独立运行）
- `assets/templates/base.html`：HTML基础模板
- `assets/colors/palettes.json`：预设配色方案
- `references/design-guide.md`：设计规范参考

导入OpenCode时，只需将 `skill.md` 内容粘贴为技能定义，并确保AI能访问其他文件（如通过文件系统或上下文）。如果AI环境不支持外部脚本，可仅依赖技能描述中的生成规则。

## 注意事项
- 如果用户未提供任何文档内容，技能应要求提供。
- 提问后必须等待所有答案，不要跳过提问直接生成。
- 严格使用文档原文和用户补充数据，不虚构内容。
- 若用户要求“提炼总结”，要确保核心信息不丢失。
- 生成后询问是否需要调整。